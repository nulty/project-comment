<!DOCTYPE html>
<html>
  <head>
    <title>Projectivity</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav class="sticky flex align-center justify-between h-[4rem] bg-teal-500">

      <div class="links">
      <% if session[:user_id].blank? %>
        <%= link_to 'Sign in', signin_path, class: 'mx-2 p-4 bg-yellow-400' %>
        <%= link_to 'Sign Up', signup_path, class: 'mx-2 p-4 bg-yellow-400' %>
      <% else %>
        <%= link_to 'Home', root_path, class: 'rounded inline-block mx-2 p-4 bg-red-500' %>
        <%= link_to 'Admin', admin_projects_path, class: 'rounded inline-block mx-2 p-4 bg-red-500' %>
        <%= link_to 'Signout', signout_path, data: { "turbo-method": :delete }, class: 'rounded inline-block mx-2 p-4 bg-red-500' %>
      </div>
      <div class="user ">
        User: <%= current_user.email %>
      </div>

      <% end %>
    </nav>
    <main class="container mx-auto mt-28 px-5 flex h-screen">
      <%= yield %>
    </main>
  </body>
</html>
